This is used to find the difference between the changes made before and after commit.

Hello world!! thats the greeting.
take the input from user, add the values.
let me modify it 


Git diff only works before stating, but if you want to check the changes in staging area, then use --staging
cant do it with commited

when you want to delete a file from git, dont delete from your working directory, but do it from git
git rm --cached filename

git branch -M main
git remote add origin 

git push -u origin main #-u means upstream...

git remote -v

added a new line